<div class="jumbotron text-center">
    <h1>Hello, Empeek!</h1>
    <p class="lead">Have a nice day!</p>
    <p><a href="" class="btn btn-primary btn-lg">get</a></p>
</div>

<div>
    <input type="text" name="paht" id="pathText" required/>
    <input type="submit" value="send" id="submit" />
</div>

<div class="container well">
    <table class="table table-condensed">
       <thead>
           <tr>
               <th>Less 10Mb</th><th>10Mb - 50Mb</th><th>More 100Mb</th>
           </tr>
       </thead>
        <tbody>
            <tr>
                <td></td><td></td><td></td>
            </tr>
        </tbody>
    </table>
    <div class="text-info">
        Current path: 
    </div>
</div>

<div id="showPathBlock">
    
</div>

<script type="text/javascript">
    $(document).ready(function () {

        $("#submit").click(function (event) {
            event.preventDefault();
            EnterPath();
        })
  
        GetAllDirectories();

        $("#showPath").click(function (event) {
            event.preventDefault();
            ShowPath();
        })
    })

    
    // geting all subbdirectories by ajax-query
    function GetAllDirectories() {
        $("#showPathBlock").css('display', 'block');
        $.ajax({
            url: '/api/values',
            type: 'GET',
            dataType: 'json',
            success: function (data) {
                WriteResponse(data);
            },
            error: function (str) {
                alert(str);
            }
        });
    }

    // enter path
    function EnterPath(currentPath) {
        var currentPath = $('pathText').val();

        $.ajax({
            url: '/api/values',
            type: GET
        })
    }

</script>